/* eslint-disable react/prop-types */
const Button_Save = ({children}) => {
    return (
        <button
            className="px-4 py-2 bg-teal-500 text-white rounded-md"
        >
            {children}
        </button>
    );
};

export default Button_Save;